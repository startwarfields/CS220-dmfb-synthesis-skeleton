NAME(output/Slicing.cfg)

DAG(DAG1)
DAG(DAG2)
DAG(DAG3)


NUMCGS(2)

COND(0, 1, DAG1, 1, DAG2, 25)       //unconditional
EXP(25, TRUE, UNCOND, DAG1)         //First DAG
TD(DAG1, 4, DAG2, 7)
TD(DAG1, 5, DAG2, 8)

COND(1, 1, DAG2, 1, DAG3, 26)       //unconditional
EXP(26, TRUE, UNCOND, DAG2)

TD(DAG2, 10, DAG3, 11)